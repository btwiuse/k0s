syntax = "proto3";

package api;

// caller: Hub <=> server: Agent/Factory::Slave
service Slave {
    rpc Send(stream Message) returns (stream Message) {};
}

message Message {
    bytes type = 1;
    bytes body = 2;
}
